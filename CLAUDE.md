# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

Git is initialized with a remote (`origin`) and history. **Phase 4 (Backend API) and Phase 5 (Frontend) are now complete.** The backend package skeleton (`backend/agents/`, `backend/graph/`, `backend/models/`, `backend/db/`, `backend/api/`, `backend/tests/`) has real logic in most of the places that matter for the simulation-and-API path, though `backend/tests/` and the frontend are still untouched. `backend/api/routes.py` now exposes three routes: `POST /match` (accepts a `MatchRequest`, returns a `job_id` immediately, then runs the full simulation as a background task that persists `simulations`/`transcripts`/`verdicts` rows to Postgres and flips status to `"completed"`/`"failed"`), `GET /match/{job_id}` (returns `{"status": ..., "job_id": ...}` while running/failed, the full verdict JSON once completed, `404` if unknown), and — as of Phase 4 Task 6 — `GET /match/{job_id}/stream` (a WebSocket: pushes `{"type": "scenario_complete", ...}` after each of the 6 scenarios finishes, then `{"type": "complete", "verdict": {...}}`, then closes; rejects unknown `job_id`s at the handshake). The WebSocket works by **polling Postgres** (not a live in-process feed) — the simulation's actual live progress exists only inside the background task's own isolated event loop, with zero pub/sub bridge to the request-serving side, so the DB is the only thing safely crossing that boundary today. The Postgres schema (`backend/db/models.py`'s `Simulation`/`Verdict`/`Transcript` SQLAlchemy 2.0 async ORM tables, `backend/db/session.py`'s async engine + `async_sessionmaker` factory, applied `alembic/` migration) is exercised by all three routes. **`backend/db/session.py`'s engine uses `NullPool`** (added during Task 5) — required because the background task's `asyncio.run()`-per-simulation pattern and any request handled on the main uvicorn event loop would otherwise share pooled asyncpg connections across incompatible event loops, which asyncpg forbids. **`requirements.txt` pins `uvicorn[standard]`** (added during Task 6) so WebSocket support (`websockets`, plus `httptools`/`watchfiles`) is a guaranteed direct dependency rather than an accident of `langgraph-sdk`/`langsmith` also needing `websockets`. **Phase 5 (Frontend) has started**: `frontend/app/page.tsx` is no longer the default scaffold — it's now a 3-step client-side questionnaire form (`"use client"`, `useState`-controlled) collecting one person's `QuestionnaireInput` fields (`sleep_schedule`, `cleanliness_level`, `guests`, `noise_tolerance`, `wfh`, `pets`, `budget`) plus a `free_text` description, matching the backend's Pydantic model field-for-field via a TypeScript `QuestionnaireInput` interface. As of Phase 5 Task 2, submit now actually calls `POST /match`: it assembles a `MatchRequest` from the real form data (side A) plus a hardcoded `TEST_PERSONA_B`/`TEST_FREE_TEXT_B` test persona (side B), stores the returned `job_id` in state, shows a loading state on the Submit button, and shows a visible error banner on failure (network error or non-2xx response, including FastAPI's default 422 validation-error shape). **`backend/api/main.py` now has `CORSMiddleware`** (added Task 2) allowing `http://localhost:3000` — required because the browser blocks the frontend's cross-origin `fetch` to the backend without it; this also covers the WebSocket stream, which needed it too once Task 3 connected. The API base URL (`http://localhost:8000`) and the second persona are both hardcoded directly in `page.tsx` for now — no env var or `.env.local` has been introduced yet. As of Phase 5 Task 3, once `job_id` is set the page opens a WebSocket to `ws://localhost:8000/match/{job_id}/stream` (via `useEffect`, cleaned up with `ws.close()` on unmount/re-run) and renders live progress: a spinner while `simulationStatus === "running"`, a list of completed scenarios (index + "Complete") built from `scenario_complete` messages, and closes the socket itself on the terminal `complete`/`failed` message rather than relying solely on the server-side close. New TypeScript types (`ObserverNotes`, `VerdictObject`, and a `WsMessage` discriminated union) mirror the backend's WebSocket payloads field-for-field — verified live against a real run, not just typed on faith. As of Phase 5 Task 4, the `complete` message's `verdict` is captured in state and rendered: `overall_summary` in a highlighted box at the top, then each of the 4 dimension scores (Lifestyle, Communication, Conflict resolution, Dealbreakers) as a labeled monochrome progress bar (`width` set via inline style, proportional to the 1-10 score) with its explanation paragraph underneath. No other frontend files have been touched — no `components/` directory exists yet, everything lives in the single `page.tsx`. A Python venv already exists at the repo root. No test suite exists yet despite being referenced below. Do not assume a file has real logic just because it exists — check its contents first. The architecture, stack, and structure described in this file are the intended design to build toward.

**Local-model support (added post-Phase-5, ahead of Phase 6):** `langchain-ollama` was added to `requirements.txt` so the pipeline can run against locally-hosted Ollama models instead of Claude/OpenAI. `backend/tests/test_local_models.py` is a standalone comparison harness — it runs the same two hardcoded, conflicting personas (an early-sleeper vs. a night-owl host) through a capped 3-scenario simulation once per local model, monkeypatching each of the 5 agent modules' `_model`/`_structured_model` globals in place (no production files touched by the test itself) and wrapping every `with_structured_output()` call in a `RetryingStructuredModel` (up to 2 retries + failure logging), since local models occasionally return output that fails Pydantic validation. Run twice — once comparing mistral vs. llama3.1, once adding qwen2.5 as a third — **llama3.1 won** on persona fidelity, structured-output reliability, and calibrated verdict scores (it was the only model to consistently score the unresolved dealbreaker low rather than clustering into a "safe" middle band). Based on that result, **`backend/agents/llm.py`'s `get_llm()` factory now centralizes LLM construction**: it reads `LLM_MODE` from the environment — `"local"` → `ChatOllama(model="llama3.1", base_url="http://localhost:11434")`, `"api"` → `ChatAnthropic(model="claude-sonnet-5")` (the existing default; `ChatOpenAI` isn't wired in since nothing in the codebase used it before this change), unset defaults to `"api"` for backward compatibility, anything else raises `ValueError`. All 5 of `persona_construction.py`/`agent_a.py`/`agent_b.py`/`observer.py`/`verdict.py` now call `get_llm()` instead of constructing `ChatAnthropic` directly. `.env`'s `LLM_MODE` was `local` through Phase 6, then switched to `api` during Phase 7 Task 6 (needed to verify 5 genuinely concurrent simulations, which a single local Ollama server can't be trusted to parallelize) and left there. **`get_llm()`'s `"api"` branch no longer forwards a `temperature` kwarg at all** (fixed during Task 6 — see Issues log): the configured `API_MODEL = "claude-sonnet-5"` rejects `temperature` outright (`400 - temperature is deprecated for this model`), so `persona_construction.py`/`verdict.py`'s `temperature=0.2` calibration (the Task 3 self-consistency fix) is currently a no-op in API mode — only still active on the `"local"`/Ollama branch. Note: with `LLM_MODE=local`, a full 6-scenario simulation will run noticeably slower than the Claude baseline (llama3.1's 3-scenario test runs took ~4-12 minutes each locally).

**Local environment quirk (this machine only, not project-level):** two native Windows PostgreSQL services (v17 on port 5432, v13 on port 5433) pre-exist outside this project and silently intercept connections meant for the docker-compose Postgres container if it publishes on either of those ports. `docker-compose.yml` therefore publishes on **port 5434** instead of the default 5432 — `DATABASE_URL` in `.env` must match. If `alembic`/the app ever gets a mysterious `InvalidPasswordError` or connects to the wrong database, check `Get-NetTCPConnection -LocalPort <port>` for a port collision before assuming the credentials are wrong.

**Phase 7 (Semantic pre-filtering with pgvector) has started, Task 1 done:** `docker-compose.yml`'s `db` service now runs **`pgvector/pgvector:pg16`** instead of plain `postgres:16` — same Postgres 16 major version, so the existing `pgdata` volume needed no reinit, just a `docker-compose up -d --force-recreate`. `backend/db/models.py` gained a new `User` model (`users` table: `id` UUID PK, `embedding` `vector(512)` nullable — 512 dims to match the not-yet-wired-in `voyage-3-lite` model, `created_at`), added via a new Alembic migration (`717928bb0789`, `down_revision='7d234afe8e5d'`) that also runs `CREATE EXTENSION IF NOT EXISTS vector`. The `pgvector` Python package (provides `pgvector.sqlalchemy.Vector`) was added to `requirements.txt`. This is intentionally minimal — no other user columns (questionnaire fields, persona data) exist yet; later Phase 7 tasks (embedding pipeline, persona wiring, seeding) will add those via their own migrations. `langchain-voyageai` is **not yet installed** — that's Task 2's job, not Task 1's.

## Project overview
AI-powered roommate compatibility system. Users complete a questionnaire and free-text description. The system constructs a behavioral persona per user, simulates two personas across multi-turn conflict scenarios using LangGraph agents, and produces a structured compatibility verdict scored across 4 dimensions via an LLM-as-judge observer pattern.

## Stack
- **Languages:** Python, TypeScript
- **Frameworks/libraries:** LangGraph, LangChain, FastAPI, Pydantic, SQLAlchemy, Next.js, React
- **Data/infra:** PostgreSQL, Claude/GPT-4o API
- **Dev tools:** LangSmith, Docker

## Architecture
```
Questionnaire + free-text
        ↓
Persona construction (LLM → PersonaObject)
        ↓
Simulation engine (LangGraph)
    Agent A ↔ Agent B (6 conflict scenarios, cross-scenario memory via checkpointer)
        ↓
Observer agent (silent, structured notes per scenario)
        ↓
Verdict layer (4-dimension score + plain-English summary)
```

Two separate LangGraph agents play Agent A and Agent B (not one agent role-playing both) — a single agent converges toward agreement, so separate agents with separate system prompts are used to produce authentic disagreement. Cross-scenario memory is carried via a LangGraph checkpointer keyed on `thread_id`. **As actually built (corrected from an earlier version of this doc):** the observer runs once per scenario (`observer_node`, Task 7), tagging each note with `scenario_index` and appending to a list — it does not see other scenarios. Cross-scenario synthesis happens one layer later, in the verdict node (Task 9), which reads the full accumulated `observer_notes` list across all scenarios. The original design's reasoning ("cross-scenario patterns are meaningful signal a per-scenario score would miss") still holds — it's just implemented at the verdict stage, not the observer stage.

## Project structure (planned)
```
roomie-match-ai/
├── backend/
│   ├── agents/          # persona_construction, agent_a, agent_b, observer, verdict
│   ├── graph/           # state.py, nodes.py, edges.py, simulation.py
│   ├── models/          # Pydantic models: persona, observer, verdict
│   ├── api/             # FastAPI app and routes
│   ├── db/              # SQLAlchemy models and session
│   └── tests/
├── frontend/
│   ├── app/             # Next.js pages: questionnaire, results
│   └── components/
├── docker-compose.yml
└── .env.example
```

## Key decisions
- **LangGraph over a loop** — StateGraph gives typed state management, checkpointing for cross-scenario memory, and conditional routing
- **Two separate agents** — single agent playing both roles converges; separate agents with separate system prompts produce authentic disagreement
- **Verdict node reads all scenarios together** (not the observer, which runs per-scenario — see Architecture note above) — cross-scenario patterns are meaningful signal a per-scenario score would miss
- **Pydantic at every boundary** — catches malformed LLM output immediately rather than letting bad data propagate
- **Async background tasks** — simulations run 30-90 seconds; synchronous endpoint would time out and block
- **PostgreSQL for transcripts** — enables self-consistency measurement and dealbreaker detection evals after the fact
- **`LLM_MODE`-driven provider swap via a single `get_llm()` factory** (`backend/agents/llm.py`) — lets Phase 6 bulk testing run cheaply/offline against a local Ollama model (llama3.1, chosen after a 3-way comparison against mistral and qwen2.5) while keeping one-line switch-back to Claude for demo-quality runs, with zero per-call-site branching

## Environment variables
```
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LANGCHAIN_API_KEY=
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=cohabit
DATABASE_URL=
LLM_MODE=
```
Never commit `.env` — only `.env.example` with key names and no values. Add `.env` to `.gitignore` before the first commit. `DATABASE_URL` on this machine is `postgresql+asyncpg://postgres:postgres@localhost:5434/roomie_match` (port 5434, not the Postgres default 5432 — see the port-collision note above). `LLM_MODE` controls which LLM provider `backend/agents/llm.py`'s `get_llm()` returns: `"local"` (Ollama/llama3.1, requires Ollama running at `http://localhost:11434`) or `"api"` (Claude, the default if unset). Currently set to `api` in `.env` (switched during Phase 7 Task 6 to verify parallel simulations against Claude; the `"api"` branch no longer accepts a `temperature` override — see Issues log).

## Running locally
```bash
# backend (from repo root — requirements.txt and venv/ live at the root, not backend/)
venv\Scripts\activate        # Windows; source venv/bin/activate on macOS/Linux
pip install -r requirements.txt
uvicorn backend.api.main:app --reload

# frontend
cd frontend
npm install
npm run dev

# database
docker-compose up -d
alembic upgrade head
```
As of Phase 7 Task 1, `docker-compose.yml`'s Postgres image is `pgvector/pgvector:pg16` (not plain `postgres:16`) — required for `alembic upgrade head`'s `CREATE EXTENSION IF NOT EXISTS vector` to succeed. If you already have a container running the old `postgres:16` image, `docker-compose up -d --force-recreate` swaps it in place without touching the `pgdata` volume.
Currently `backend/api/main.py` exposes `GET /` (health check, with `CORSMiddleware` allowing `http://localhost:3000`) and, via `backend/api/routes.py`'s `APIRouter`: **as of Phase 7 Task 6**, `POST /match` now takes **one** `questionnaire`+`free_text` (not two), synchronously constructs+embeds+stores that persona and runs `find_top_matches()` to select 5 candidates from `users`, then backgrounds all 5 six-scenario simulations in parallel (one per candidate) and returns `{"matches": [{"job_id", "candidate_id"} x5]}` immediately after — persisting `simulations`/`transcripts`/`verdicts`/`matches` rows to Postgres. `GET /match/{job_id}` (poll once for the result — unchanged, works per-job_id exactly as before) and `GET /match/{job_id}/stream` (WebSocket — unchanged) both still operate on any one of the 5 job_ids independently. `alembic upgrade head` creates `simulations`/`verdicts`/`transcripts`/`users`/`matches` in Postgres. **As of Phase 7 Task 7, the frontend (`frontend/app/page.tsx`) is fully wired to this new shape**: submits the single-sided request, then opens 5 simultaneous WebSockets (one per returned `job_id`) and renders a ranked grid of up to 5 match cards, each filling in independently as its own socket completes. Which LLM provider backs all of this is controlled by `.env`'s `LLM_MODE` (see Environment variables above) — currently `api` (Claude; switched from `local` during Task 6).

`backend/tests/` now has real content, not just a `.gitkeep`: `test_simulation.py` (a full 6-scenario end-to-end assertion test, self-invoking at import) and `test_local_models.py` (the mistral/llama3.1/qwen2.5 comparison harness described above). Both are run directly (`python -m backend.tests.test_simulation` / `python -m backend.tests.test_local_models` from the repo root — the `-m` form is required since `backend/` has no top-level `__init__.py`), not via `pytest` — `pytest` still isn't in `requirements.txt` and no pytest config exists.

## Development guidelines
- Explain reasoning before implementing — architectural decisions should be made before writing code
- Build incrementally — smallest working unit first, verify it runs, then expand
- One task per session — finish and commit before moving to the next thing
- Never move forward until the current piece actually runs correctly

## Metrics tracking

Key metrics to record during development and testing. Update this section as numbers come in — these feed directly into resume bullets and interview talking points.

### Simulation scale
- Total simulated user pairs run end-to-end: `7` (Jordan/Riley — Tasks 6, 7, 10, and reused throughout Phase 2/3 verification; Sam/Casey — Task 11; an auto-named pair generated live from `POST /match` request payloads — Phase 4 Task 2 verification; a second auto-named pair, "Elena" vs. "Jordan" — Phase 4 Task 4 verification, first pair whose full result was persisted to Postgres; a third auto-named pair — Phase 4 Task 5's first verification attempt, which never got a `simulations` row due to the cross-event-loop `asyncpg` bug (see Issues log) and was abandoned mid-run; a fourth auto-named pair, "Elin" vs. "Milo" — Phase 4 Task 5's successful re-verification after the `NullPool` fix; a fifth auto-named pair, "Elena" vs. "Remy" — Phase 4 Task 6's WebSocket-streaming verification). Note: 3 additional personas (Mara, Jamie, Dana) were generated during the Phase 3 persona-construction audit but never run through the simulation graph — they test `construct_persona()` in isolation, not end-to-end pairs.
- Total simulation runs (including repeats for consistency testing): `13` full/partial graph runs (2 single-scenario runs in Task 6, 1 in Task 7, 1 three-scenario run in Task 10, 1 full six-scenario run in Task 11, 1 three-scenario rerun for the LangSmith-setup check, 1 three-scenario rerun for the run_name check, 2 single-scenario runs for the Phase 3 agent-consistency controlled test, 1 full six-scenario run in Phase 4 Task 2, 1 full six-scenario run in Phase 4 Task 4, 1 full six-scenario run in Phase 4 Task 5, 1 full six-scenario run in Phase 4 Task 6 — streamed live over the new WebSocket and confirmed byte-for-byte against the DB). Not counted as a run: Task 5's first attempt crashed during the very first DB write (before any LLM call), so no graph execution happened
- Phase 7 additional simulated pairs: `11` new full 6-scenario runs beyond the Phase 1-5 totals above — Task 3 verification (1 pair, a real `POST /match` submission under the then-current `LLM_MODE=local`, before the Task 6 switch to `api`); Task 6 verification (5 candidate pairs run **in parallel** via the new single-profile `/match` endpoint, `LLM_MODE=api`); Task 7 verification (5 more candidate pairs, same endpoint, driven live through a real headless-Chromium browser session). Separately, `75` synthetic personas were generated via `backend/tests/seed_users.py` (Task 4) to populate the `users` candidate pool — these are embedded personas, not simulated pairs; no conflict-scenario graph ever runs between them, so they're not counted here.
- Total LangSmith traced LLM calls across all runs: `~251` through end of Phase 6 (reconstructed from this session's actual tool calls — see breakdown below; the LangSmith dashboard's own project-level stats are the authoritative source per the measurement note below, this session's build-up is provided as a cross-check). **Phase 7 added substantially more** (75 seeded personas × 1 Haiku call + 1 Voyage embed call each; Task 6/7's 11 additional simulated pairs at ~33 calls each per the established per-simulation rate above; several standalone `construct_persona()`/`embed_persona()` calls during Tasks 2/3/5 verification) but wasn't reconstructed call-by-call the way Phases 1-6 were — the LangSmith dashboard is the authoritative source for the Phase 7 total, not tracked here
  - Task 3 (persona construction, incl. the `method="json_schema"` bug fix): 3
  - Task 4 (Agent A): 2
  - Task 5 (Agent B): 1
  - Task 6 (single-scenario demo, run twice): 8
  - Task 7 (observer, chained off a fresh graph run): 5
  - Task 9 (verdict, fixture-based, no live graph run): 1
  - Task 10 (3-scenario full graph): 16
  - Task 11 (6-scenario full graph): 31
  - Post-Task-11 `name=` field verification: 2
  - LangSmith-setup verification (3-scenario rerun): 16
  - `run_name` verification (3-scenario rerun): 16
  - Phase 3 persona-construction audit (3 cases, before the prompt fix): 3
  - Phase 3 persona-construction audit (same 3 cases, after the prompt fix): 3
  - Phase 3 agent-consistency controlled test (2 single-scenario runs): 12
  - Phase 4 Task 2 (background-task wiring verification, 1 full 6-scenario run via `POST /match`, counted directly from server-log `httpx` request lines): 33
  - Phase 4 Task 4 (persistence verification, 1 full 6-scenario run via `POST /match`, counted directly from server-log `httpx` request lines): 33
  - Phase 4 Task 5 (results-endpoint verification, 1 full 6-scenario run via `POST /match` after the `NullPool` fix — the aborted first attempt made 0 LLM calls since it crashed before persona construction): 33
  - Phase 4 Task 6 (WebSocket-streaming verification, 1 full 6-scenario run via `POST /match`, streamed and cross-checked against the DB): 33

### Performance
- p95 verdict delivery time end-to-end (job start → verdict stored), `claude-haiku-4-5-20251001` at 3 scenarios: **p50 111.1s / p95 127.8s / max 136.7s** across 50 runs, 0 outliers above 3x p95 — measured via `backend/tests/measure_latency.py` from `simulations.created_at`/`completed_at` (both DB-timestamped). Filtered to exactly the 50 `job_id`s in `backend/tests/results/test_pairs.json` rather than querying all completed `simulations` rows: live-checked the table and found 76 completed rows total, not 50 — the extra 26 are earlier Phase 4 6-scenario Sonnet verification runs plus the original (superseded) `measure_consistency.py` v1's 20 runs, and the schema has no column recording model/scenario count, so blending them in would have silently corrupted the number. **Caveat: this is Haiku/3-scenario, not the production Sonnet/6-scenario configuration** — see the two prior data points below for the only real Sonnet/6-scenario measurements so far (206s, 275s), which aren't a distribution/p95 on their own
- Average simulation duration across all runs: `~206-275s` (two measured data points: Phase 4 Task 2's 206s via log timestamps, Phase 4 Task 4's 275s via DB `created_at`/`completed_at` timestamps — the latter is now the more authoritative source going forward since it's stored, not just logged). Task 10/11's runs were observed to take multiple minutes each but weren't stopwatched precisely. LangSmith's per-trace latency (e.g. the 191.54s P50/P99 seen for one turn in Task 11's trace) is per-LLM-call, not per full simulation
- Max concurrent simulations handled without degradation: `5` — first real data point, from Phase 7 Tasks 6/7. Discovered live (by reading Starlette's own installed source) that FastAPI's `BackgroundTasks` does **not** run multiple registered tasks concurrently — `BackgroundTasks.__call__` is `for task in self.tasks: await task()`, strictly sequential even for threadpooled sync tasks. True concurrency required wrapping all 5 simulations in one `asyncio.gather()` inside a single `add_task()` registration. Once implemented: all 5 simulations in Task 6's verification reached `"completed"` within a ~100-second window of each other (20:08:13 → 20:09:52), vs. an estimated 15-25+ minutes if genuinely sequential, based on this project's own prior 206-275s-per-simulation timing. Task 7 replicated this with a second live run. Not tested beyond 5 concurrent simulations, and not tested under multiple simultaneous *client* submissions (every test so far has been one `POST /match` call at a time)

### Quality
- Agent self-consistency score (same two personas, same scenario, two runs — verdict similarity): **80.0%** (16/20 consistent), corrected methodology, up from an initial **50.0%** (10/20) — see below for both. Matching criteria for both measurements: the dominant compatibility outcome (mean of the 4 scores, bucketed low ≤4 / medium 5-7 / high ≥8 — fixed thresholds matching the verdict prompt's own designed scale, not fit to observed data) AND the exact set of most-problematic dimension(s) (lowest-scoring of the 4, ties included) both had to align across runs — an operationalization of this doc's "flagged dealbreakers align" methodology below, adapted because stored results only carry the final `VerdictObject` scores, not raw `dealbreaker_violations` text.
  - **Initial measurement (50.0%, superseded):** `backend/tests/measure_consistency.py` v1 resubmitted the first 20 of the 50 `test_pairs.json` pairs through `POST /match` a second time. Diagnosis (by pulling actual observer notes for the worst-swinging pairs straight from Postgres via `job_id`) found the dominant variance source wasn't verdict-judge miscalibration — it was `persona_construction()` inventing a different name *and* meaningfully different dealbreaker substance on every call given the identical questionnaire/free_text input (e.g. pair 10's run 2 never discussed dishes at all, a dealbreaker that dominated an entire scenario in run 1, because that run's regenerated persona simply didn't have a dish-related dealbreaker). This conflated persona-construction variance with conversation/judge variance into one number.
  - **Corrected measurement (80.0%, current):** two production fixes plus a redesigned test. Fixes: added an optional `temperature` param to `get_llm()` (`backend/agents/llm.py`), set to `0.2` for `persona_construction.py` and `verdict.py` specifically (dealbreaker substance and judge calibration should be more faithfully derived from input, less "creative"), left at default for `agent_a.py`/`agent_b.py`/`observer.py` (roleplay dialogue keeps natural variation). Test redesign: `measure_consistency.py` now constructs each pair's `PersonaObject`s **once**, then invokes the compiled graph directly twice (different `thread_id`, no `POST /match`, no Postgres) — holding persona identity fixed matches this metric's original "same two personas, two runs" definition literally, and isolates conversation+verdict variance from persona-construction variance now that the latter is controlled for. Result: 16/20 consistent, and notably all 4 remaining inconsistent pairs (2, 3, 6, 9) had *matching* most-problematic dimensions across both runs (`['dealbreaker']` both times in every case) — the only remaining disagreement is the aggregate-severity low/medium bucket boundary (e.g. pair 9's mean moved 2.5 → 5.5, a uniform +3 across all 4 scores). That points to `agent_a`/`agent_b` dialogue variance (still default temperature, by design) as the one remaining untuned variance source.
- Dealbreaker detection precision before/after prompt iteration: **not measured — Phase 6 Task 4 skipped.** This metric assumed a "Phase 3 iterated observer prompt" to compare against the original. Checked via `git log -p` on `backend/agents/observer.py` before building anything: its `SYSTEM_PROMPT` text has never changed since first written — only its model-construction wiring changed later (direct `ChatAnthropic` → `get_llm()` factory). This matches the "Number of prompt iterations to reach final observer prompt: `0`" line directly below — there is, and has only ever been, one observer prompt. Rather than retroactively writing a second prompt just to manufacture a before/after number, the task was dropped instead of building on a false premise.
- Number of prompt iterations to reach final observer prompt: `0` (audited in Phase 3 against 3 concrete criteria — specific quotes vs. vague commentary — and found already solid from the first version; no changes made)
- Number of prompt iterations for persona construction: `1` (Phase 3 audit found inference quality degraded to near-restatement on flat/single-note inputs; added an explicit good-vs-bad example plus contradiction-detection guidance; re-verified improvement on the same 3 test cases)
- Number of prompt iterations for agent system prompts: `1` (Phase 3 added an anti-sycophancy-drift instruction so agents don't cave on dealbreakers just to stay agreeable; verified with a controlled same-pair/same-scenario/twice comparison)

### Local model evaluation (pre-Phase-6 infra)
Two independent 3-scenario comparison runs via `backend/tests/test_local_models.py`, same hardcoded persona pair both times (early-sleeper vs. night-owl host), against the local Ollama server:

| Run | mistral | llama3.1 | qwen2.5 |
|-----|---------|----------|---------|
| Run 1 (2-model) | 418.5s, scores 7/9/8/5, 0 structured-output failures | 724.7s, scores 4/5/2/1, 1 failure (self-recovered via retry) | not run |
| Run 2 (3-model) | 279.2s, scores 6/8/7/5, 0 failures | 235.0s, scores 4/5/3/2, 0 failures | 255.9s, scores 7/6/8/9, 0 failures |

(Score order: lifestyle/communication/conflict/dealbreaker.) **llama3.1 selected as the Phase 6 local model** — across both runs it was the only model to consistently score the unresolved dealbreaker low (1-2) rather than clustering into a "safe" mid-range band the way mistral did both times (dealbreaker 5 twice) and qwen2.5 did once (dealbreaker 9, despite dialogue showing the same unresolved conflict both other models flagged — a likely miscalibration, only one data point so far). llama3.1 was also fastest in Run 2, though slowest in Run 1 — timing wasn't consistent enough across runs to be a deciding factor on its own.

### Phase 7: Semantic matching & parallel simulation
- Embedding model: `voyage-3-lite` via `langchain-voyageai`, `512` dimensions — confirmed live against the real Voyage API (`backend/agents/embeddings.py`), matches the `vector(512)` pgvector column exactly.
- Candidate pool size: `77` `users` rows (75 synthetic, seeded via 6 personality archetypes with `random.seed(42)` for reproducibility; 2 real, from live `POST /match` submissions during Task 3/6/7 verification) — every row has both `persona` (JSONB) and a non-null `embedding`.
- Candidate selection verified against real cosine-similarity output, not synthetic distances: a fresh test persona returned similarities in the ~0.50-0.54 range against the pool; an exact copy of an existing seeded persona correctly excluded itself (no self-match ≥ 0.999999) and returned its 5 nearest *other* archetype-siblings at 0.82-0.93 similarity — confirming the exclusion logic works on real data, not just in theory.
- Ranked-match score spread (Task 7 live browser verification, 5 real candidates): `65% / 65% / 50% / 40% / 35%` compatibility — real variance across the 5 candidates rather than clustering at one value, consistent with this project's broader anti-score-clustering design goal (see Quality section above) now holding at the candidate-ranking level too.
- Voyage AI's free tier hard-caps embedding requests at `3` RPM without a payment method on file — discovered live when the unpaced seeding script failed on user 4/75 (see Issues log). Fixed with a 21-second pacing delay between calls; not expected to be a concern for real traffic (at most one embed call per real `POST /match` submission).

### How to measure each one
**Self-consistency score** — pick 20 persona pairs, run each through the same scenario twice, compare the verdicts side by side. Score as matching if the dominant compatibility outcome and flagged dealbreakers align. `(matching runs / total runs) * 100`

**Dealbreaker detection precision** — manually label 30 simulation transcripts as "dealbreaker present" or "not present." Run the observer output against your labels. `(true positives / (true positives + false positives)) * 100`

**p95 latency** — log `job_start_time` and `verdict_stored_time` for every simulation. Sort all durations, take the 95th percentile value.

**LangSmith call count** — visible in the LangSmith dashboard under project stats after all test runs complete.

## Progress tracking

Update this section at the end of every task and phase. This is the first thing a new Claude Code session reads to understand where the project stands — keep it current.

### Current status
- **Current phase:** none — Phase 7 (Semantic pre-filtering with pgvector) is closed out; all planned project phases are now done
- **Current task:** none
- **Last completed task:** Phase 7, Task 7 — Update frontend results page. `frontend/app/page.tsx` rewired off the removed two-sided `questionnaire_a`/`questionnaire_b` `POST /match` shape onto the new single `questionnaire`+`free_text` request; on a successful submit it now opens **5 simultaneous WebSocket connections** (one per `job_id` from `MatchResponse.matches`, reusing the existing unchanged `/match/{job_id}/stream` endpoint and its existing `WsMessage`/`ScenarioCompleteMessage`/`CompleteMessage`/`FailedMessage` types verbatim — only the *number of sockets* and *per-job state* changed from Phase 5's single-socket version). Each card renders independently the moment its own socket reports `complete`/`failed`, closing only that socket (matching both the server's own per-job close-on-completion behavior and Phase 5's existing client-side precedent) — ranking (`#1`-`#5` badges, sorted by average of the 4 dimension scores) is a derived value computed only once every result has settled, not stored in state. Verified live in a real headless-Chromium run (no `chromium-cli`/Playwright available in this environment, so installed Playwright as a temporary dev dependency, drove the actual UI end-to-end, then uninstalled it — `frontend/package.json` confirmed back to its exact committed state afterward): submitted the form, confirmed 200 on `POST /match`, captured a screenshot mid-run showing 4 cards still spinning while 1 already displayed its full result (proving genuine progressive per-card loading, not wait-for-all), and a final screenshot showing all 5 cards re-sorted descending (65%/65%/50%/40%/35%) with rank badges and zero browser console errors. **Observed but did not fix** (out of scope for a frontend task): one verdict's `overall_summary` contained visible LLM template-leakage text (`"placeholder text will be overwritten below in final fields..."`) — a pre-existing `backend/agents/verdict.py` prompt-quality issue, not a frontend bug (the page correctly rendered whatever string the backend returned) — flagged for a future prompt-iteration pass, not addressed here.
- **Previously completed task:** Phase 7, Task 6 — Update `/match` endpoint for candidate-based matching. `POST /match` now takes one `questionnaire`+`free_text`, runs `find_top_matches()` to select 5 candidates, and backgrounds all 5 six-scenario simulations in genuine parallel (via a single `BackgroundTasks.add_task()` wrapping `asyncio.gather()` — confirmed by reading Starlette's source that multiple separate `add_task()` calls run strictly sequentially, not in parallel). New `matches` table links each submission to its 5 candidate simulations + similarity scores. Verified live: 5 distinct job_ids returned in ~23s, all 5 simulations completed within a ~100-second window of each other.
- **Next task:** none scoped — no Phase 8 exists in this doc's planned structure. Any further work would need to be scoped fresh

### Phase completion
- [x] Phase 1 — Core simulation in isolation
- [x] Phase 2 — Observability
- [x] Phase 3 — Prompt iteration
- [x] Phase 4 — Backend API
- [x] Phase 5 — Frontend
- [x] Phase 6 — Testing and metrics *(Task 4 skipped — see progress notes)*
- [x] Phase 7 — Semantic pre-filtering with pgvector

### Phase 1 tasks
- [x] Task 1 — Pydantic models
- [x] Task 2 — LangGraph state
- [x] Task 3 — Persona construction function
- [x] Task 4 — Agent A node
- [x] Task 5 — Agent B node
- [x] Task 6 — Single scenario conversation
- [x] Task 7 — Observer node
- [x] Task 8 — Scenario routing
- [x] Task 9 — Verdict node
- [x] Task 10 — Full graph assembly with checkpointer
- [x] Task 11 — End-to-end test with two distinct personas

### Phase 2 tasks
- [x] Task 1 — Langsmith setup
- [x] Task 2 — Name your runs

### Phase 4 tasks
- [x] Task 1 — FastAPI app skeleton
- [x] Task 2 — Background task
- [x] Task 3 — PostgreSQL setup
- [x] Task 4 — Persist simulation results
- [x] Task 5 — Results endpoint
- [x] Task 6 — Websocket streaming

### Phase 5 tasks
- [x] Task 1 — Questionnaire form
- [x] Task 2 — Wire form to backend
- [x] Task 3 — Simulation progress
- [x] Task 4 — Verdict display

### Phase 6 tasks
- [x] Task 1 — Generate 200+ test pairs *(scoped down to 50 — see progress notes)*
- [x] Task 2 — Measure self-consistency
- [x] Task 3 — Measure p95 latency

### Phase 7 tasks
- [x] Task 1 — Add pgvector to PostgreSQL
- [x] Task 2 — Embedding pipeline
- [x] Task 3 — Wire embeddings into persona construction
- [x] Task 4 — Seeding script
- [x] Task 5 — Candidate selection function
- [x] Task 6 — Update match endpoint
- [x] Task 7 — Update frontend results page



### Decisions log
Record any architectural decisions made during the build that weren't in the original design. Format: decision made, why, any tradeoffs accepted.

| Decision | Reason | Tradeoff |
|----------|--------|----------|
| `with_structured_output(..., method="json_schema")` instead of the default `method="function_calling"` for Claude structured output | Default tool-calling method let Claude return `dealbreakers`/`behavioral_traits` as a single string instead of a list, failing Pydantic validation, even after adding `Field(description=...)` hints | `json_schema` mode is Anthropic-specific structured-output enforcement; if another provider is ever swapped in, this method choice needs revisiting |
| `PersonaObject.name` is invented by the LLM rather than passed in | `construct_persona()`'s signature only takes `questionnaire` + `free_text`, no name field, but `PersonaObject.name` is required | Persona names aren't user-supplied — fine for simulation purposes, would need revisiting if personas are ever shown to end users under a real identity |
| Keep Claude's extended thinking enabled on agent nodes (`agent_a.py` and `agent_b.py`), instead of disabling it | Extended thinking's visible reasoning is wanted later for a frontend dashboard view and for inspecting reasoning quality in LangSmith traces | `AIMessage.content` from `ChatAnthropic` is a list of content blocks (`{"type": "thinking", ...}` + `{"type": "text", ...}`), not a plain string. A text-extraction helper (`_extract_text()`) was needed sooner than predicted — added in `backend/graph/simulation.py` at Task 6, not Task 5/7 as originally guessed, since the relay nodes between the two agents needed plain text before either Agent B or the observer did |
| Added two "relay" nodes (`_relay_a_to_b`, `_relay_b_to_a`) in `backend/graph/simulation.py` between `agent_a_node`/`agent_b_node` in the graph | `agent_a_node`/`agent_b_node` are constrained to only read/write their own `messages_a`/`messages_b` (a hard constraint from Tasks 4-5), so with no glue node between them, Agent B would never see what Agent A said and vice versa — two disconnected monologues instead of a conversation. Routing/conditional-edge functions can't mutate state, so this had to be a node, not edge logic | Two extra hops per turn in the graph; each relay also owns advancing `turn_count`, so turn-counting logic lives outside both agent nodes |
| Added `turn_count: int` to `SimulationState` (`backend/graph/state.py`) | Task 6 needed "stop after N turns" logic; the alternative (deriving turn count from `len(messages_a) + len(messages_b)`) was rejected as fragile — it would silently break if the seeding/relay logic changes and double-counts each turn (one `AIMessage` + one relayed `HumanMessage` per turn) | Reopened an already-completed file (`state.py`) for a field not in the original Task 2 design — same category of change as the `PersonaObject.name`/`Field(description=...)` situation in Task 3 |
| `observer_node()` manually appends to `observer_notes` (`existing + [new_note]`) instead of adding an `operator.add` reducer to `state.py` | `observer_notes` has exactly one writer, unlike `messages_a`/`messages_b` which needed `add_messages`'s ID-based merge logic; a plain reducer would be no more correct than the node managing the full list itself | `state.py` left untouched this task — but this means observer-related state now uses a different accumulation mechanism than the message fields, worth knowing before adding more list-accumulating state fields |
| Applied `with_structured_output(..., method="json_schema")` to `ObserverNotes` from the start, before hitting any failure | `ObserverNotes` has four `list[str]` fields — the exact shape that broke `PersonaObject` under the default `function_calling` method in Task 3 | None — this is the established fix from Task 3, applied proactively rather than rediscovered |
| `ObserverNotes.scenario_index` is force-set via `.model_copy(update={"scenario_index": ...})` after the LLM call, not trusted from the model's structured output | The model has no reliable way to know the true `current_scenario` value, and Task 3 already established the model can't be trusted to reproduce structured fields correctly | None — deterministic override, no downside found |
| Split scenario routing into two functions: `should_continue()` (pure, string-returning) and `increment_scenario_node()` (state-mutating) in `backend/graph/edges.py`, instead of one function doing both | Confirmed directly against LangGraph docs that conditional-edge routing functions cannot update state — only nodes (or `Command`-returning nodes) can. The task's own "done when" also required `should_continue()` to return a plain string, which a `Command`-based single-function design couldn't satisfy | Task 10 must remember to run `increment_scenario_node` immediately before the conditional edge that calls `should_continue`, or the scenario counter never advances and the graph loops forever |
| Corrected a stale claim in this doc's Architecture/Key decisions sections: "observer reads all 6 scenarios together" was never actually true once Task 7 was built to spec (observer runs once per scenario, tagged with `scenario_index`) | Task 9 (verdict node) is what actually reads the full `observer_notes` list across all scenarios — cross-scenario synthesis happens one layer later than the original design doc said | None functionally — the original reasoning (cross-scenario patterns matter) still holds, it's just implemented at the verdict stage; corrected the doc rather than leaving it describing an architecture that was never built |
| `verdict_node()`'s system prompt explicitly names and counteracts LLM score-clustering (instructs full 1-10 range use, reserves 8-10 for unambiguous evidence, maps each of the 4 dimensions to specific evidence categories) | Spec called out a real, well-documented LLM-as-judge failure mode — scores defaulting to a narrow "safe" band regardless of actual variance | Verified against 3 fixture scenarios (smooth / mixed / unresolved-dealbreaker) rather than a live 6-scenario run, to avoid pre-empting Task 11's end-to-end test with ~50+ premature LLM calls |
| Task 10's outer scenario loop (agent_a → agent_b → observer → verdict) runs as **one continuous graph execution with internal loop-back edges**, under a single `thread_id` — not as separate `.invoke()` calls per scenario sharing a `thread_id` | The task spec's own wording had two constraints in tension ("conditional edge → agent_a again" implies one graph run; "each scenario invocation uses the same thread_id" implies separate calls). The literal, detailed graph-topology wording was treated as the stronger signal | **Confirmed with the user** (post-Phase-3 audit) that this is the intended design — the checkpointer's role here is mostly future-proofing (resumability) rather than load-bearing for a single continuous run. No rework needed |
| Added `_start_next_scenario_node` and a `SCENARIO_PROMPTS` list (6 conflict topics) in `simulation.py`, not in the original Task 10 spec | Looping back to `agent_a` with no glue node would leave `turn_count` at its prior value (breaking the inner 4-turn loop after scenario 1) and never introduce a new conflict topic | None — necessary for the outer loop to function at all once more than one scenario runs |
| Renamed `_should_continue` (Task 6, the inner 4-turn-loop check) to `_should_continue_turn` | Task 10 also imports Task 8's `should_continue` (the outer scenario-loop check) into the same file — two identically-named, differently-scoped functions in one module was a real readability hazard | None — internal rename only, not part of any task's "done when" |
| Demo in `simulation.py`'s `__main__` seeds `current_scenario=3` instead of `0` to get a 3-scenario run | Running the full 6 scenarios for this task's own verification would mean ~40+ live LLM calls, duplicating Task 11's explicit job. Reusing `edges.py`'s unmodified `TOTAL_SCENARIOS=6` by starting partway through avoids touching that already-verified file | None — `current_scenario` is just a progress counter, starting it at 3 is functionally identical to starting at 0 with a lower ceiling |
| Progress printed via `graph.stream(..., stream_mode="values")` in the driver code instead of a print-only node baked into the graph | `Workflow.md`'s own guidance treats debug prints as development-time, not committed graph structure | None found |
| Set `name` on messages in `agent_a_node`/`agent_b_node` (own `AIMessage` reply) and the relay nodes (relayed `HumanMessage`) in `simulation.py` | Post-Task-11, LangSmith traces only showed generic `human`/`ai` role labels instead of persona names, making traces harder to read. Confirmed via docs that LangChain messages support a `name` field for exactly this, with a provider-dependent-behavior caveat | Verified live against Anthropic specifically (not assumed from docs) — the field is accepted without error and round-trips correctly through a full relay → next-agent call; `observer_node`/`verdict_node` messages are unaffected since those aren't persona-voiced turns |
| Added `config={"run_name": ...}` to the `.invoke()` call inside each node (`agent_a_scenario_{n}`, `agent_b_scenario_{n}`, `observer_scenario_{n}`, `verdict_synthesis`) — separate and distinct from the message-level `name` field added earlier | "Name your runs" (Phase 2 Task 2) turned out to mean naming the individual LLM-call span, not the message speaker label already handled. The two are genuinely different LangSmith concepts: `run_name` labels a trace span/row; message `name` labels a speaker within message content | Verified live in the trace tree: the graph node name (e.g. `agent_a`, from `add_node`) and the nested `run_name` (`agent_a_scenario_3`) both appear, correctly nested rather than one overwriting the other |
| Strengthened `persona_construction.py`'s `SYSTEM_PROMPT` with an explicit good-vs-bad example pair, guiding questions for behavior-in-conflict, and an instruction to treat free-text contradictions as inference signal | An evidence-based audit (3 diverse `construct_persona()` calls — a flat/single-note input, a socially-open input, a self-contradicting input) found inference quality was inconsistent: strong when the input naturally contained tension, but degraded to near-restatement on flatter inputs (e.g. `"comfortable with high noise levels"` ≈ a direct paraphrase of `noise_tolerance=9`) | Re-ran the same 3 cases after the change: the previously-weak flat-input case improved the most (e.g. `"comfortable with high noise levels"` → `"may genuinely fail to notice when a roommate is bothered, requiring the roommate to speak up first"`), and also fixed an unrelated accuracy issue where `cleanliness_level=3` had been softened to `"moderate"` |
| Added an anti-sycophancy-drift instruction to `agent_a.py`/`agent_b.py`'s `_build_system_prompt()`: do not soften or abandon a stated dealbreaker just because the other party is agreeable or the conversation has gone pleasantly | Targeted a known LLM failure mode (gradually caving to be agreeable over a long back-and-forth) that hadn't been explicitly guarded against, directly relevant to "stay in character under pressure" | Verified with a deliberate controlled test (not an incidental rerun): same pair (Jordan/Riley), same single scenario, seeded identically, run twice independently. All 4 verdict scores landed within 1 point across both runs, the dominant conclusion matched ("workable but fragile, hinges on follow-through" in both), and in both runs Jordan flexed on enforcement mechanics (dropping a hard cutoff for a vibe-based standard) while never abandoning the underlying dealbreaker itself — still 1 pair/1 scenario/2 runs, not the full 20-pair statistical validation `CLAUDE.md`'s Quality metrics describe |
| `run_simulation()` (the `POST /match` background task) lives directly in `backend/api/routes.py` rather than a new `backend/services/` module | The task is small and self-contained (build 2 personas, assemble initial state, invoke the existing compiled `graph`, log twice) — a new module would be premature structure for one function, and no service layer exists yet elsewhere in the codebase | None — may need revisiting once Phase 4 Task 4 (persist simulation results) adds more logic to the same task function |
| `thread_id` for the LangGraph checkpointer is set to `str(job_id)` per request, instead of a hardcoded string (as the `simulation.py` demo uses) | Each `POST /match` call needs isolated checkpointer state under the single shared in-memory `MemorySaver()` instance; reusing one hardcoded thread id across requests would let concurrent runs collide | None found — `job_id` is already a UUID generated per request, so reusing it as the thread key added no new field |
| Introduced stdlib `logging` for the first time in this repo (`logging.basicConfig(...)` in `backend/api/main.py`, `logging.getLogger(__name__)` in `routes.py`), instead of continuing the bare-`print()` convention `simulation.py`'s demo/tests use | Server background-task output needs to show up in `uvicorn`'s log stream with timestamps; `print()` has no level/timestamp/logger-name structure and doesn't compose with `uvicorn`'s own logging | `%(asctime)s` was added to the format string specifically so "log start time"/"log completion time" didn't need manual `datetime` calls in the log messages themselves |
| `Simulation`/`Verdict`/`Transcript` (`backend/db/models.py`) use `UUID(as_uuid=True)` with client-side `default=uuid.uuid4` for all PKs, `DateTime(timezone=True)` (not the implicit naive `datetime` type-map) for timestamps, `Text` (not `String`) for the 5 LLM-generated verdict prose fields, `JSONB` for `transcripts.content`, and `ondelete="CASCADE"` on both simulation FKs | UUID matches the existing `job_id = uuid.uuid4()` pattern and avoids depending on Postgres's `pgcrypto` extension; `DateTime(timezone=True)` avoids silently dropping the tz info that Postgres's `func.now()` (a `timestamptz`) actually returns; `Text` avoids truncation risk on unpredictable-length LLM output; `JSONB` is indexable/queryable, matching CLAUDE.md's stated eval use case; `CASCADE` prevents orphaned audit rows if a simulation is ever deleted | None of these are currently exercised by write paths (Task 4 will be the first real test); `CASCADE` is a DDL-level choice that can be changed via another migration if it ever proves wrong for the audit-trail use case |
| `backend/db/session.py` reads `DATABASE_URL` via `os.environ["DATABASE_URL"]`, not `os.getenv(...)` with a fallback | Fails loudly (`KeyError`) on misconfiguration rather than silently connecting to the wrong database — directly relevant after this task's own debugging saga where a wrong-seeming connection error actually had nothing to do with the DSN itself | None — matches the "fail fast" instinct the port-collision investigation reinforced |
| Used Alembic's `-t async` template (`alembic init -t async alembic`) rather than hand-writing async migration plumbing | Confirmed via Alembic's current docs (context7) that the async template already generates the correct `async_engine_from_config` + `connection.run_sync(...)` + `asyncio.run(...)` boilerplate for async DBAPIs — asyncpg is the only driver installed (no sync psycopg2/psycopg), so this path is mandatory, not optional | `alembic/env.py` still needed a `sys.path.insert(...)` safety net so `from backend.db.models import Base` resolves, since `backend/` has no top-level `__init__.py` (implicit namespace package) and Alembic's `env.py` runs as a standalone script |
| `docker-compose.yml` publishes Postgres on port **5434** instead of the default 5432 | Two pre-existing native Windows PostgreSQL installs on this dev machine (v17 on 5432, v13 on 5433 — confirmed via each install's `postgresql.conf`) silently intercepted all host connections meant for the docker-compose container on either port, producing misleading `InvalidPasswordError`s. Confirmed the real cause by testing auth from *inside* Docker's own network (`docker run --network cohabit_default ... psql -h db`), which succeeded — proving the container/credentials were fine and the failure was purely a Windows host-networking port collision | User explicitly chose remapping the port over stopping either native service, to avoid any risk to whatever those native installs are used for outside this project. This is a per-machine workaround, not a project design choice — a different dev machine wouldn't need it |
| `run_simulation()`'s outer signature/registration stayed an unchanged sync `def` passed to `background_tasks.add_task`; its body became a single `asyncio.run(_run_simulation_async(...))` wrapping ALL real logic (persona construction, graph streaming, every DB write) in one event loop for the run's full lifetime, rather than converting the outer function to `async def` directly or calling `asyncio.run()` separately per DB write | `AsyncSession`/`async_sessionmaker` can only be used from a coroutine, so *some* async entry point was unavoidable. Converting the outer function to `async def` and registering it directly with FastAPI would have moved its blocking work onto the shared uvicorn event loop (background tasks registered as coroutines are awaited there, not threadpooled) — a real regression from today's isolation. Calling `asyncio.run()` separately per write (once for the "running" insert, once per scenario transcript, once for the verdict) was also considered and rejected: asyncpg connections aren't loop-portable, so handing a pooled connection from one now-closed event loop to the next `asyncio.run()`'s new loop risks "attached to a different loop" errors | None found — this is a strictly better structure than either alternative, not a compromise |
| Switched `graph.invoke(...)` to `async for state in graph.astream(initial_state, config, stream_mode="values")` to detect scenario completion | Needed a way to observe state after every node (to catch each scenario's `observer_notes` entry as it's produced) instead of only getting the final state — exactly the pattern the `simulation.py` `__main__` demo already established for progress printing, just switched to the async variant since `_run_simulation_async` is now a coroutine. No changes needed to any node function (`agent_a_node` etc. stay sync) — LangChain's Runnable protocol runs sync callables via a thread executor when invoked asynchronously | None — even in the hypothetical worst case where that internal threading didn't happen, correctness is unaffected since this event loop is private to one background-task thread, never shared with the rest of the app |
| `transcripts.content` stores the scenario's `ObserverNotes` dict as-is (`scenario_index`, `friction_points`, `dealbreaker_violations`, `tone_shifts`, `concessions`), not combined with a raw per-scenario dialogue slice | **User's explicit choice** between two options presented: observer-notes-only vs. observer-notes-plus-raw-dialogue. Raw dialogue would have required tracking message-list boundaries between scenarios (the graph accumulates `messages_a`/`messages_b` across all 6 scenarios for cross-scenario memory, with no existing per-scenario slice markers) — real added complexity with no stated requirement driving it. Observer notes alone already match CLAUDE.md's stated purpose for this table (self-consistency/dealbreaker evals) and are available for free exactly when scenario completion is detected | If raw dialogue per scenario is ever needed later (e.g. for a frontend transcript viewer), that's a schema/graph change, not a query-layer one — would need the graph to track per-scenario message boundaries explicitly |
| Simulation status transitions use `sqlalchemy.update(Simulation).where(Simulation.id == job_id).values(...)` (Core-style statement) rather than fetching the ORM row and mutating it | Each write happens in its own short-lived session (`async with async_session_factory() as session: ...`), so the row created in an earlier session/request isn't attached to the current session's identity map — an `update()` statement avoids needing a redundant `SELECT` just to get an object to mutate | None — this is strictly simpler for the "fire-and-forget status flip" use case here |
| `GET /match/{job_id}` returns the `{"status": ..., "job_id": ...}` shape for **any** non-`"completed"` status (including `"failed"`), not just `"running"` | The task spec only described `"running"`/`"completed"`, leaving `"failed"` (a status the background task can genuinely set) unhandled. Generalizing "not done yet/successfully → return what's known" to cover both non-terminal and failed-terminal states needed no extra branching and avoids inventing a third, undiscussed response shape | None — if a distinct "failed" response shape is ever wanted (e.g. an error detail field), that's a small additive change, not a breaking one |
| No `response_model=` declared on `GET /match/{job_id}` | The two success shapes (a plain status dict vs. the full `VerdictObject`) aren't unifiable under one Pydantic response model without either a discriminated union or losing the exact shapes the task specifies. FastAPI serializes whatever object is returned (dict or Pydantic instance) correctly without one | Slightly weaker auto-generated OpenAPI schema for this route than routes with a declared `response_model`; acceptable since there's no OpenAPI-consuming client yet |
| `backend/db/session.py`'s `create_async_engine(...)` uses `poolclass=NullPool` | Discovered live (see Issues log) that the default pooled engine is unsafe once more than one asyncio event loop context uses it: the background task's `asyncio.run()`-per-simulation pattern creates a fresh loop each run, while routes like the new `GET /match/{job_id}` run on the shared main uvicorn loop — asyncpg connections are bound to the loop they were created on and can't be handed across. `NullPool` opens a fresh connection per checkout and closes it on checkin, so no connection is ever reused across a loop boundary | Loses connection-reuse performance (a new TCP+auth handshake per query) — negligible at this app's current traffic volume (one simulation at a time, occasional polling), but worth revisiting if concurrency or query volume grows enough that connection-establishment overhead becomes measurable |
| `GET /match/{job_id}/stream` (WebSocket) works by **polling Postgres every 1.5s** rather than building an in-process pub/sub bridge between the background task's isolated event loop and the main uvicorn loop | **User's explicit choice** between the two options presented. The simulation's live `graph.astream(...)` state exists only inside `_run_simulation_async`'s own event loop, with zero existing queue/pub-sub/`app.state` infrastructure to surface it elsewhere; the only thing already crossing that thread/loop boundary safely is Postgres itself (via the `Transcript`/`Simulation` rows the background task already commits). Polling reuses that fully-tested path with zero new concurrency primitives, at the cost of ~1-2s latency between a scenario finishing and the message arriving | If sub-second push latency is ever needed, that requires new infrastructure (e.g. `asyncio.Queue` + `run_coroutine_threadsafe`, or a real broker) — a bigger, separate change, not a tweak to this one |
| Unknown `job_id` on `GET /match/{job_id}/stream` is rejected via `raise WebSocketException(code=status.WS_1008_POLICY_VIOLATION)` **before** `websocket.accept()` | Confirmed via FastAPI's current docs (context7) that this is the idiomatic way to reject a WebSocket connection at the handshake — cleaner than accepting the connection just to immediately close it, and mirrors the semantic of the GET endpoint's `404` at the equivalent layer for this protocol | None — verified live: connecting to an unknown `job_id` returns HTTP 403 at the handshake, never an accepted-then-closed connection |
| `"failed"` status on the stream sends `{"type": "failed", "job_id": ...}` then closes, instead of leaving the connection open indefinitely or crashing | Same spec gap as Task 5's GET endpoint (task only described `"scenario_complete"`/`"complete"`) — this is the direct analogue using the message-type convention this task establishes, rather than Task 5's flat status-object shape | None — a client can distinguish `"complete"` from `"failed"` by `type` alone |
| `requirements.txt` changed from bare `uvicorn` to `uvicorn[standard]` | Discovered live that `websockets` (required for FastAPI's `WebSocket` support to function at all) was only present in the venv as an incidental transitive dependency of `langgraph-sdk`/`langsmith`, not of `uvicorn` itself — a future removal of either of those would silently break WebSocket support with no warning | None — `httptools`/`watchfiles` (the extra's other deps) are a strict improvement (faster HTTP parsing, better `--reload` file watching), not just dead weight |
| Questionnaire form (`frontend/app/page.tsx`) groups the 7 `QuestionnaireInput` fields into exactly 3 steps (basic info / lifestyle preferences / free text), uses `<select>` for `sleep_schedule`/`guests`/`budget` and range sliders for `cleanliness_level`/`noise_tolerance`, and keeps all field names snake_case in TypeScript rather than converting to camelCase | Backend enforces no enum/`Literal` on the 3 string fields — the step grouping and select-option sets are frontend-only decisions with no schema to copy from. Range sliders directly mirror the backend's explicit 1-10 validator. Keeping snake_case means the object can serialize straight into the future API request body with zero mapping layer | The `sleep_schedule`/`guests`/`budget` option sets (`early_bird`/`night_owl`/`flexible`, etc.) are invented, not backend-derived — worth revisiting if the backend ever adds real enum constraints that don't match |
| Split into two TypeScript interfaces — `QuestionnaireInput` (exact 7-field match to the Pydantic model) and `QuestionnaireFormData extends QuestionnaireInput { free_text: string }` — instead of one combined interface | The task required "a TypeScript interface matching QuestionnaireInput exactly," which a combined interface with `free_text` baked in would violate | None — trivial extra interface, and it directly documents that `free_text` is an addition on top of the backend model, not part of it |
| Single-person questionnaire form built first, not a two-person `MatchRequest`-shaped form | The task's own field list ("all QuestionnaireInput fields plus a free_text field") is singular — matches one `QuestionnaireInput` + one `free_text`, not the two-sided `MatchRequest` shape `POST /match` actually needs. Treated as the intentional scope for this task, consistent with the project's incremental "smallest working unit" pattern | A second person's data collection and the two-sided `MatchRequest` assembly still need to be built — likely Phase 5 Task 2 ("Wire form to backend") |
| Fixed CORS via `CORSMiddleware` in `backend/api/main.py` rather than a Next.js `rewrites()` proxy in `next.config.ts` | **User's explicit choice** between the two options presented. A rewrite would keep the change frontend-only, but doesn't cleanly proxy WebSocket upgrades — and a later Phase 5 task will need the frontend to open a WebSocket to `/match/{job_id}/stream`. Backend middleware solves CORS once for every future frontend→backend call (fetch and WebSocket alike) | Backend code now has a frontend-origin dependency (`http://localhost:3000` hardcoded in `allow_origins`) — fine for single-origin local dev, would need a real allowlist/env-var-driven config before any deployment |
| API base URL (`http://localhost:8000`) and the second persona (`TEST_PERSONA_B`/`TEST_FREE_TEXT_B`) are both hardcoded consts directly in `page.tsx`, no `.env.local`/`NEXT_PUBLIC_*` var introduced | Matches the task's own "for now hardcode the second user profile" framing — no existing env-var infrastructure in the frontend to extend, and introducing one for a single URL string would be premature for this task's scope | Both will need to become real (env-var-driven base URL, real second-user data flow) before this app is usable beyond local dev/testing |
| `handleSubmit`'s error handling parses the response body's `detail` field if present (FastAPI's default 422 validation-error shape) but falls back to a generic `Request failed with status ${status}` message otherwise | No custom error-response shape exists anywhere in the backend (confirmed via exploration) — a 422 has `{"detail": [...]}`, but 5xx/network errors won't, so the handler can't assume one fixed shape | Verified the 422 shape live against a real malformed request rather than assuming FastAPI's default; the fallback path (network error, 500, etc.) relies on standard `fetch()`/`Error` semantics, not separately live-tested |
| Post-success UI swaps to a confirmation view (job id shown) instead of leaving the filled-out step form on screen | Re-submitting the same form data once a `job_id` already exists doesn't make sense — avoids double-submission confusion for near-zero added complexity | None — trivial conditional render, no new state needed beyond the `jobId` that already had to be tracked |
| Each `scenarios` list entry always renders a "Complete" badge, with no intermediate per-scenario status value tracked | The backend only ever emits `scenario_complete` after a scenario has actually finished — there's no "in progress" state for an individual scenario the client could observe, only the overall `simulationStatus` spinner for "not all scenarios done yet" | None — matches the data the server actually sends; inventing a richer per-scenario status would have no backing data |
| `onmessage`'s `complete`/`failed` handlers call `ws.close()` explicitly, on top of the server already closing the socket right after sending those messages | Directly satisfies the task's literal "close WebSocket connection when complete message arrives" constraint rather than relying on the server-side close being sufficient — also correct defensively if the server-side close is ever delayed or the message arrives just before a slow close | None — closing an already-closing/closed `WebSocket` is a no-op per spec, no risk of a double-close error |
| A `verdict` state (`useState<VerdictObject | null>`) was added per the plan then removed after `npm run lint` flagged it as an unused variable | This task's constraints don't require displaying the verdict (that's Task 4) — storing it "for later" produced genuine dead code by this task's own scope, caught by lint rather than assumed away | Task 4 will need to re-add equivalent state when it actually renders verdict fields — a small, expected bit of re-work, not a design mistake |
| Verdict score bars are monochrome (matching the existing multi-step progress indicator's zinc fill/track colors), not red/yellow/green threshold-colored | Task explicitly said "keep it clean and minimal," and no color-coding precedent exists anywhere else in the page — introducing a new color system for one section would be inconsistent with the rest of the app's neutral palette | A future task could add threshold coloring (e.g. red below 4, green above 7) if the plain numeric+bar isn't legible enough at a glance; deferred since it wasn't asked for |
| Score bar fill width set via inline `style={{ width: `${score * 10}%` }}` rather than a Tailwind utility class | Tailwind's `className` approach only expresses widths it can see at build time (fixed classes or a safelisted arbitrary-value set) — a genuinely data-driven per-render percentage has no clean Tailwind-only expression, so inline style is the standard, pragmatic choice here | None — this is the accepted pattern for truly dynamic values in a Tailwind codebase, no build-time safelist needed |
| The 4 score/explanation blocks are rendered by `.map()`-ing a small local array of `{label, score, explanation}` built from the `verdict` object, instead of writing out 4 near-identical JSX blocks by hand | Each block is multi-line (label+score row, bar, paragraph) — real duplication across 4 near-identical copies, not the "three similar lines" case where inline repetition is preferable to an abstraction | None — this is a plain local array + map inside the render, not a new component/file/prop-drilling — appropriately scoped to this one render, not a premature abstraction |
| `test_local_models.py` swaps in `ChatOllama` by **monkeypatching each agent module's `_model`/`_structured_model` globals at runtime** rather than editing `agent_a.py`/`agent_b.py`/`observer.py`/`verdict.py`/`persona_construction.py` | Every one of those 5 modules builds its own private module-level `_model = ChatAnthropic(...)` at import time with no factory/DI seam at the time this test was written; Python resolves module globals at call time, so reassigning the attribute before `graph.stream(...)` is picked up by the already-compiled graph with zero production-file edits | Superseded for production use once `get_llm()`/`LLM_MODE` was added — the test script still uses monkeypatching (now patching over whatever `get_llm()` returned at import) since it needs to swap between 3 *local* models in one process, which `LLM_MODE` (a single global on/off switch) doesn't support |
| `RetryingStructuredModel` (test-only wrapper class in `test_local_models.py`) wraps each local model's structured-output Runnable with up to 2 retries + failure logging, rather than editing the production node functions to add retry logic | Local models are a known source of schema-invalid structured output (confirmed live — see Issues log); wrapping only in the test file keeps 0 production-file behavior change for the existing Claude/OpenAI path | Confirmed live and load-bearing, not just theoretical: llama3.1's Run 1 verdict call returned `dealbreaker_score: 0` (outside the 1-10 validator range), was caught, retried, and succeeded on attempt 2 without crashing the run |
| `get_llm()` (`backend/agents/llm.py`) defaults to `"api"` when `LLM_MODE` is unset, rather than requiring it (matching `DATABASE_URL`'s established fail-fast-on-missing convention) | `LLM_MODE` is new and additive — any existing `.env` file without it should keep working exactly as before (Claude), not suddenly crash every LLM call. An invalid *value* (anything other than `"local"`/`"api"`) still raises `ValueError`, so typos aren't silently swallowed | Slightly inconsistent with `DATABASE_URL`'s stricter "always required" pattern — deliberate, since a wrong/missing `DATABASE_URL` has one obviously-correct value per environment, while `LLM_MODE` has a reasonable default (whatever the app already did before this feature existed) |
| `get_llm()`'s `"api"` branch returns only `ChatAnthropic`, with no `ChatOpenAI` branch despite `langchain-openai` being a listed dependency | Confirmed via `grep` that no file in the codebase ever actually constructed `ChatOpenAI` — `OPENAI_API_KEY`/the dependency exist but are unused. Adding a real `ChatOpenAI` branch would need a second env var (which provider within "api" mode) that was never specified | If OpenAI is ever wanted as a real second API option, that's a small additive change to `get_llm()`, not a redesign |
| `get_llm()` gained an optional `temperature: float \| None = None` param (omitted → provider default, unchanged behavior for existing callers), and `persona_construction.py`/`verdict.py` now call it with `temperature=0.2` while `agent_a.py`/`agent_b.py`/`observer.py` stay on the default | Diagnosed as part of the self-consistency investigation (see Quality metrics / Progress tracking): `persona_construction()` was regenerating meaningfully different dealbreaker substance (not just phrasing) from identical input on every call, a bigger consistency problem than anything in the verdict prompt itself. Lowering temperature only on the two "should be faithfully derived from input, not creative" stages (persona substance, judge scoring) while leaving the three roleplay/analysis stages alone preserves the natural conversational variation the project's dialogue is supposed to have | Self-consistency score went 50.0% → 80.0% after this change (combined with holding personas fixed in the test itself — see Progress tracking). This is a production behavior change, not just test tooling — real user-submitted matches now get more deterministic persona construction and verdict scoring than before |
| Phase 7 Task 1's `users` table was created **minimal** (`id`, `embedding`, `created_at` only), not with questionnaire/persona columns the seeding script (Task 4) will likely need | **User's explicit choice** between two options presented (minimal now vs. fuller schema now to avoid a second migration soon). Matches the project's established "smallest working unit" incremental convention (see Development guidelines) | Task 4 (seeding script) will need its own migration to add whatever columns it needs — a small, expected follow-up, not a design mistake |
| `docker-compose.yml`'s `db` image was swapped from `postgres:16` to `pgvector/pgvector:pg16` rather than keeping `postgres:16` and installing pgvector via a custom Dockerfile | **User's explicit choice** between the two options presented. Same Postgres 16 major version means the official pgvector image is a drop-in swap with the existing `pgdata` volume, vs. a custom Dockerfile adding a new build-context/Dockerfile-maintenance surface for the same end result | None found — official image tracks upstream pgvector releases; a hand-maintained Dockerfile would need manual version bumps |
| The new migration's `downgrade()` drops only the `users` table, not the `vector` extension itself | Extensions are database-level infrastructure, not owned by any one table — `CREATE EXTENSION IF NOT EXISTS` is safely idempotent for any later migration that also needs it, while `DROP EXTENSION` risks breaking anything else that depends on it once other things do. Same convention used for `postgis`/`uuid-ossp` migrations elsewhere | If `users` is ever fully torn down and pgvector genuinely needs uninstalling too, that's a separate, deliberate migration — not bundled into this one's downgrade path |
| `embed_persona()` (`backend/agents/embeddings.py`) uses `VoyageAIEmbeddings.embed_query()`, not `.embed_documents()` | Confirmed via current LangChain docs that `embed_query(text: str)` returns a single vector directly while `embed_documents(texts: list[str])` returns a batch (`list[list[float]]`) — since `embed_persona()` takes exactly one `PersonaObject` and must return exactly one vector, `embed_query` is the direct-fit method with no list-wrapping/indexing needed | Voyage AI's API distinguishes "query" vs. "document" input types for asymmetric retrieval quality tuning; this wasn't specified by the task and isn't load-bearing for symmetric persona-to-persona similarity, but worth revisiting once Task 5 (candidate selection) actually runs cosine similarity — if quality is off, trying `embed_documents` consistently for all stored personas is the first thing to test |
| Persona data is stored in `users` as one `persona JSONB` column (`PersonaObject.model_dump()` as-is), not as individual typed columns (separate `name`/`sleep_schedule`/`conflict_style`/etc. columns) | **User's explicit choice** between the two options presented. Mirrors the existing `Transcript.content` JSONB pattern already established for structured LLM output in this codebase; one migration instead of seven new typed columns | Individual fields aren't independently queryable/indexable without JSON operators (`persona->>'name'`) — acceptable since nothing in Phase 7 so far needs to filter/sort by a specific persona field, only to read the whole object back and embed it |
| Every `construct_persona()` call in `POST /match` now creates a brand-new `User` row (2 per match, fresh `uuid4()` id each), with no lookup/dedup against existing rows | No `user_id`/identity/auth concept exists anywhere in the app yet — `Simulation` has no FK to `users`, there's no signup/login flow, nothing to look up or deduplicate against. This was the only coherent option, not a choice among alternatives | Every match submission grows `users` unboundedly with no reuse, even for a literal identical resubmission — acceptable for now since Phase 7's goal is a searchable candidate *pool*, not user accounts; would need real identity (e.g. session/auth-tied `user_id`) if this table is ever meant to represent actual persistent people rather than one-off simulation participants |
| `construct_persona()` itself was left completely unmodified — the embed+store logic lives only in `routes.py`'s call site, not inside the function | `construct_persona()` is also called from `backend/tests/test_local_models.py` and `backend/tests/measure_consistency.py`, both of which have zero DB session infrastructure and would break if embed+store were baked into the function itself. Matches the codebase's existing convention: all DB writes for this flow already live in `routes.py`, never inside the agent modules | None — this is strictly additive at the one call site that has both a DB session and Postgres-backed identity; the two test scripts are completely unaffected, still produce plain in-memory `PersonaObject`s |
| `seed_users.py` forces Claude Haiku 4.5 by monkeypatching `persona_construction._model`/`_structured_model` (restored via `finally`), rather than adding a model-override param to `get_llm()` | `get_llm()`'s `"api"` branch is hardcoded to `API_MODEL = "claude-sonnet-5"` with no per-call model-name override, and `.env`'s `LLM_MODE=local` means calling `construct_persona()` unmodified would route through local Ollama, not Haiku. `test_local_models.py` already established this exact monkeypatch pattern for swapping in Ollama models; reusing it for Haiku keeps the real production prompt/logic (DRY) with zero production-file changes | If a script ever needs to force a specific *API* model regularly (not just for one-off local-model comparisons or seeding), that recurring need would justify actually adding a model-override param to `get_llm()` instead of monkeypatching each time |
| `seed_users.py` generates `free_text` via string templates per archetype, not a second LLM call | The task required *programmatic* generation for the `QuestionnaireInput` fields; extending the same programmatic approach to `free_text` keeps the script to one LLM call per user (persona construction) instead of two, and the task only required free_text to "match" the questionnaire answers, not that it be LLM-authored | Templated text is less varied in phrasing than an LLM would produce — acceptable since `embed_persona()` only reads the LLM-inferred `PersonaObject` fields (`behavioral_traits`/`conflict_style`/`communication_style`/`dealbreakers`), not `free_text` directly, so template repetitiveness doesn't propagate into the embeddings themselves |
| `find_top_matches()` (`backend/agents/candidate_selection.py`) uses pgvector-python's `User.embedding.cosine_distance()` SQLAlchemy comparator instead of a raw `text()` query, despite the task handing over a literal raw-SQL string as a constraint | Confirmed via current pgvector-python docs (context7) that `.cosine_distance()` is the documented comparator, compiling to the same `<=>` operator the raw SQL describes, while staying in the ORM so `Vector`'s own type decorator handles list↔pgvector conversion automatically — the raw SQL was treated as a description of intended query shape, not a literal `text()` mandate | None found — same underlying SQL semantics either way; the ORM form is what every other query in this codebase already uses |
| Self-exclusion ("exclude the new user if they exist in the database") is implemented by over-fetching one extra row (`LIMIT 6`) and filtering out any result with `similarity >= 1 - 1e-6`, rather than accepting a `user_id` parameter | The function's fixed single-argument signature (`persona: PersonaObject`, per the task's own "done when" showing a single-arg call) has no id to look up or exclude by. `embed_persona()` is deterministic for identical input text, so an already-stored persona's nearest neighbor is itself at similarity ≈ 1.0 — filtering that out is the only signal available from a persona-only input | Verified live both ways: a fresh, never-stored persona → no filtering triggered, 5 real matches at ~0.50-0.54 similarity; an exact copy of an existing seeded user's persona → the self-row was correctly excluded, returning 5 other archetype-sibling matches at 0.82-0.93. Known unhandled edge case: two genuinely different stored personas producing near-bit-identical embeddings would over-filter — not defended against, vanishingly unlikely with real embedding data |
| `/match`'s 5 parallel simulations are dispatched via **one** `background_tasks.add_task()` call whose target runs `asyncio.gather()` over all 5, instead of 5 separate `add_task()` calls | Confirmed by reading the installed Starlette source (`starlette/background.py`) that `BackgroundTasks.__call__` does `for task in self.tasks: await task()` — strictly sequential, regardless of sync tasks being dispatched via `run_in_threadpool`. Five separate `add_task()` calls would run the 5 simulations one after another (~5x wall-clock), directly failing the task's own "done when: all 5 simulations run in parallel." The one-`add_task`-wrapping-`gather` form is the only way to satisfy both "use BackgroundTasks" and "in parallel" simultaneously | Verified live: all 5 simulations reached `"completed"` within a ~100-second window of each other, not serialized — confirms real concurrency |
| `create_match` stayed a plain sync `def` (not `async def`), doing `construct_persona()`/`embed_persona()`/`find_top_matches()` synchronously before returning, rather than backgrounding candidate selection too | The response must include real `candidate_id`s per job_id immediately, which requires persona construction + embedding + `find_top_matches()` to have already run — none of that can be deferred if the client needs it in the response body. Keeping the route a sync `def` lets FastAPI's automatic threadpooling (the same mechanism already used for sync background tasks) absorb that ~20s of blocking work without stalling the main uvicorn event loop, matching this codebase's existing "sync function bare-calls blocking work, FastAPI/asyncio.run handles the isolation" pattern rather than inventing a new one | `POST /match` is now measurably slower than before (~20-25s instead of near-instant) — an accepted, necessary tradeoff, not a regression to fix; the actual expensive work (5 six-scenario simulations) is still fully backgrounded |
| Frontend (Task 7) opens 5 independent WebSocket connections and closes each one on its own `complete`/`failed` message, rather than waiting for all 5 before closing any | Matches the server's own behavior (`stream_match` already closes each socket right after sending its terminal message, unchanged since Phase 4) and the exact client-side pattern Phase 5 already established for the single-socket case, just replicated per-job. By the time all 5 are actually done, all 5 sockets are — as a natural consequence — already closed; no extra "wait for all, then close" coordination was needed to satisfy that requirement | Verified live: zero lingering-connection issues, zero console errors, and the mid-run screenshot confirms sockets complete and close independently (4 still open/spinning while 1 had already closed with a rendered result) |
| Frontend ranking (`#1`-`#5`, sort by average of the 4 dimension scores) is a value derived in render from `results` state, not stored in its own state variable | `results` already fully determines the ranking (`status`, `verdict` per job) — a separate `rankedResults` state would just be a redundant, potentially-stale copy requiring manual sync. Recomputing on every render is cheap (5 items) | None — standard "derive, don't duplicate" React practice |
| Used a temporary Playwright install (not `chromium-cli`, unavailable in this environment) to visually verify Task 7's UI in a real browser, then fully uninstalled it afterward | This is a UI-behavior task (progressive per-card loading, correct re-ranking) that genuinely cannot be confirmed by reading code or hitting the API directly — needed a real rendered page. No browser-automation tool existed in this session, and the project has no existing frontend test/E2E infrastructure to extend, so adding Playwright permanently would be introducing unrequested project structure | `npm uninstall playwright` afterward, confirmed via `git diff` that `frontend/package.json` matched its committed state exactly with no leftover devDependency. If this project wants durable E2E coverage later, that's a deliberate infra decision to make explicitly, not a side effect of one verification pass |

### Issues log
Record bugs or problems encountered and how they were resolved. Useful for interviews — being able to talk about what broke and how you fixed it is as valuable as the working system.

| Issue | Root cause | Resolution |
|-------|------------|------------|
| `construct_persona()` raised `pydantic_core.ValidationError` on `dealbreakers`/`behavioral_traits` (`Input should be a valid list`, got a string) | Claude's tool-calling structured-output path doesn't always respect `list[str]` typing in the tool schema — it collapsed multiple items into one combined string. Adding `Field(description=...)` to those fields did not fix it. | Switched `with_structured_output` to `method="json_schema"`, Anthropic's stricter schema-enforced output mode; fixed on first retry |
| `alembic revision --autogenerate` and direct `asyncpg` connections both failed with `asyncpg.exceptions.InvalidPasswordError: password authentication failed for user "postgres"`, despite `.env`'s `DATABASE_URL` exactly matching `docker-compose.yml`'s declared `POSTGRES_PASSWORD` | Two separate root causes stacked: (1) the Postgres data volume had been initialized once before (`docker ps` showed the container up for 25+ min prior, logs showed "Skipping initialization" from 2026-08-02) under different credentials — Postgres only applies `POSTGRES_PASSWORD` on first-ever volume init, so the env var was silently ignored on restart; (2) after fixing that via `ALTER ROLE`, the error persisted because two native Windows PostgreSQL services (v17 on port 5432, v13 on port 5433) were intercepting the host's connections before they ever reached the container — confirmed by the complete absence of any corresponding `FATAL`/auth log line in `docker logs`, and confirmed conclusively by successfully authenticating from *inside* Docker's own network (bypassing Windows host networking) with the exact same credentials that failed over the host port | Fixed root cause (1) via `ALTER ROLE postgres WITH PASSWORD 'postgres'` (safe — confirmed the DB had zero tables/data at the time). Fixed root cause (2) by remapping `docker-compose.yml`'s published port to 5434 (first tried 5433, which turned out to itself collide with the second native install — checked port availability via `Get-NetTCPConnection` before landing on 5434) |
| First live test of `GET /match/{job_id}` (Phase 4 Task 5) crashed the background task with `RuntimeError: ... got Future ... attached to a different loop`, raised from deep inside `asyncpg`'s connection/transaction code, the moment `_run_simulation_async` tried its very first DB write (`Simulation(id=job_id, status="running")`) — the row was never created, so every immediate `GET /match/{job_id}` returned `404` even though `POST /match` had just succeeded | `backend/db/session.py`'s `engine` used SQLAlchemy's default pooled `AsyncAdaptedQueuePool`, shared as a module-level singleton. The background task's `run_simulation()` calls `asyncio.run(_run_simulation_async(...))`, which creates a brand-new event loop per simulation; meanwhets, `GET /match/{job_id}` (and any other route) runs on the shared, persistent main uvicorn event loop. Both draw connections from the same pool. `asyncpg` connections are bound to the specific event loop they were opened on and cannot be reused from a different one — once the pool had handed out a connection under one loop, a later checkout from a different loop crashed. Task 4's own design notes had already flagged this exact hazard *within* a single simulation's multiple writes (and fixed it there by consolidating to one `asyncio.run()` per run) but didn't anticipate a second, concurrent consumer (a request-serving route) hitting the same shared pool from an entirely different loop | Added `poolclass=NullPool` to `backend/db/session.py`'s `create_async_engine(...)` call — eliminates connection reuse entirely, so no connection is ever tied to a stale/foreign loop. Restarted the server, reran `POST /match` + polling `GET /match/{job_id}` end-to-end with a fresh pair; confirmed the `simulations` row now appears immediately and the full verdict is returned correctly on completion, with zero errors in the log |
| While adding `uvicorn[standard]` to `requirements.txt` (Phase 4 Task 6), running `pip install -r requirements.txt --upgrade` unintentionally bumped `alembic` (1.18.5 → 1.19.0), `langsmith` (0.10.15 → 0.10.16), and `langchain-anthropic` (1.5.3 → 1.5.4) — none of which this task needed touched | `requirements.txt` pins no versions at all, so `--upgrade` reinstalls the latest available version of every package in the file, not just the one being added/changed. The `--upgrade` flag was reached for out of habit, not because it was actually needed — a plain `pip install -r requirements.txt` (no flag) would have installed only the genuinely-missing `[standard]` extras (`httptools`, `watchfiles`) without touching already-satisfied packages | Reinstalled the three affected packages back to their exact previous versions (`pip install "alembic==1.18.5" "langsmith==0.10.15" "langchain-anthropic==1.5.3"`), confirmed `httptools`/`watchfiles` were untouched and still present, and `pip check` reported no broken requirements — before considering the task's dependency change done |
| First run of `test_local_models.py` crashed immediately with `ModuleNotFoundError: No module named 'backend'` when invoked as `python backend/tests/test_local_models.py` | Same root cause as the pre-existing Alembic `sys.path` quirk noted above: `backend/` has no top-level `__init__.py` (implicit namespace package), so running the file directly as a script doesn't put the repo root on `sys.path` the way a package-relative import needs | Reran as `python -m backend.tests.test_local_models` from the repo root instead — resolves correctly since `-m` adds the current directory to `sys.path`. Documented in the Running Locally section as the required invocation for both test files |
| llama3.1's verdict-synthesis call (Run 1 of the local-model comparison) failed structured-output validation: `Failed to parse VerdictObject ... dealbreaker_score: 0 ... must be between 1 and 10` | The model correctly judged the conflict as maximally unresolved but picked `0` instead of respecting the Pydantic `1-10` range the schema requires — a real local-model schema-compliance gap, not a transient/network error | Caught by `test_local_models.py`'s `RetryingStructuredModel` wrapper (logged the failure, retried), and the retry succeeded on attempt 2 with a valid score. No production impact since this only affects the local-model eval path, but confirms the retry/logging requirement in that script's design was necessary, not defensive-programming theater |
| A stray `scratchpad_test_local_models_output.log` (output captured via `tee` while running the local-model comparison from a terminal) ended up sitting untracked in the repo root, and `.gitignore` had no pattern that would have caught it or any future `*.log` file | `.gitignore` only listed specific known artifacts (`.env`, `__pycache__/`, `*.pyc`, `venv/`, `.next/`, `node_modules/`, `.DS_Store`, `interview-guide.md`) with no general log-file rule | Deleted the stray file (untracked, safe to remove) and added `*.log` to `.gitignore` so future ad hoc `tee`/redirect output doesn't risk being staged into a commit |
| `seed_users.py`'s first live run crashed on the 4th user with `voyageai.error.RateLimitError: You have not yet added your payment method... reduced rate limits of 3 RPM and 10K TPM` | Voyage AI's free tier (no payment method on file for this account) caps requests at 3/minute; the initial script called `embed_persona()` in a tight sequential loop with no pacing, burning through the quota in under a minute | Deleted the 3 partially-seeded rows from the failed run (kept the dataset clean rather than leaving near-duplicate early-archetype rows from a second seed of the same `random.seed(42)` sequence), added a module constant `EMBED_REQUEST_INTERVAL_SECONDS = 21` and a `time.sleep(21)` after every `embed_persona()` call except the last, and reran. Full 75-user run completed cleanly in ~26 minutes with zero further rate-limit errors — confirms 21s spacing reliably stays under the 3 RPM cap |
| First live `POST /match` under `LLM_MODE=api` (Phase 7 Task 6) failed with `anthropic.BadRequestError: 400 - temperature is deprecated for this model` | `get_llm()`'s `"api"` branch always forwarded whatever `temperature` kwarg the caller passed (`persona_construction.py`/`verdict.py` pass `0.2`, the Task 3 self-consistency fix) straight into `ChatAnthropic(...)`, but the configured `API_MODEL = "claude-sonnet-5"` has deprecated the `temperature` parameter entirely. This had never been exercised live before — every prior live test in this session ran with `LLM_MODE=local` (Ollama), which does accept `temperature` | Changed `get_llm()` so the `temperature` kwarg is only ever forwarded on the `"local"`/Ollama branch; the `"api"` branch now constructs `ChatAnthropic(model=API_MODEL)` with no `temperature` at all. This silently drops the Task 3 self-consistency calibration for API-mode persona construction/verdict scoring — flagged as a known regression, not something this task tried to fully solve (no alternative determinism lever identified for this model in the time available); worth a follow-up if API-mode self-consistency is re-measured |
| `_prepare_match`'s single-transaction insert of 5 `Simulation` + 5 `Match` rows (interleaved `session.add()` calls) failed with `IntegrityError: ... violates foreign key constraint "matches_simulation_id_fkey" ... Key (simulation_id)=(...) is not present in table "simulations"` | SQLAlchemy's flush batched all 5 `Match` inserts into one multi-row `INSERT ... VALUES (...), (...), ...` statement and executed it before the corresponding `Simulation` batch, despite the `Simulation`/`Match` `session.add()` calls being interleaved per-candidate in the loop — no ORM `relationship()` was declared between the two classes for SQLAlchemy to infer insert-order dependency from | Split into two separate committed transactions: one `async with async_session_factory() as session:` block inserting all 5 `Simulation` rows and committing, then a second block inserting all 5 `Match` rows — matching this file's pre-existing convention of one micro-session per logical write rather than relying on same-transaction ordering. Confirmed the failed attempt left zero orphaned rows (full transaction rollback) before retrying |
| Live end-to-end verification (Phase 7 Task 7) surfaced a verdict whose `overall_summary` contained visible LLM template-leakage text — `"placeholder text will be overwritten below in final fields..."` mixed in with real content, `...ict_score explanation continues here.'," ` trailing off mid-sentence | Not investigated in depth (out of scope for a frontend-only task) — appears to be `backend/agents/verdict.py`'s system prompt occasionally leaking its own internal scaffolding/instructions into the `overall_summary` field rather than being fully overwritten with real analysis. The frontend rendered the string correctly; this is a verdict-generation quality issue, not a display bug | Not fixed. Flagged here so it isn't lost — a future prompt-iteration pass on `verdict.py` (the project's own `SYSTEM_PROMPT` iteration convention, per the Decisions log entries for `persona_construction.py`/`agent_a.py`/`agent_b.py`) should look at whether this recurs and whether it's specific to `LLM_MODE=api`'s current model or the prompt's own wording |
| Right after swapping to `pgvector/pgvector:pg16` (Phase 7 Task 1), the very first `psql` query against the existing `roomie_match` database printed `WARNING: database "roomie_match" has a collation version mismatch` (created under collation version 2.41, OS now provides 2.36) | The old `postgres:16` and new `pgvector/pgvector:pg16` images are built on different base-OS glibc versions; Postgres stores the collation library version a database was created under and warns when the running OS's version has drifted, since text sort order (and therefore any index relying on default collation) could silently change | Ran the exact command Postgres's own hint suggested: `ALTER DATABASE roomie_match REFRESH COLLATION VERSION`. Safe here — the only tables using default-collation text columns (`simulations.status`, `verdicts`' `Text` columns, etc.) have no collation-sensitive indexes, so there was nothing to rebuild, just the stored version stamp to reconcile |
